# openmem 写入规矩

> 拿到这一份，才有资格往 openmem 里写东西。**规矩是准入条文，不是说明书。**

## 写入前四步

1. **先搜** —— `mh_search(关键词, requester="<你的 agent 名>")`，再换 1~2 个近义词搜一遍。库里可能已经有了。
2. **有则原地改** —— `mh_update(id, content=…)`。**保留 id，不新增。**
3. **无则新增** —— 确认真的没有，才 `mh_write`。
4. **同主题只维护一条「活条目」** —— 版本 / 状态变了就 update 那一条，禁止一版一条流水账。

## 写错了怎么办

1. **能改就别删** —— `mh_update(id, content=…)` 保留 id，不打断引用链。
2. **确认两条是同一件事、增量已并入保留条** → `mh_deprecate(id=下线条, supersede=保留条)` 标 `superseded_by`（检索/查重/体检一律忽略 = 等同下线，`restore=true` 可反悔）。🔴 取代关系必须**落字段**：只在正文写「本条取代 X」机器读不到，X 会照样活着被查出来当新知识。
3. **要无痕清除**（仅限老大点名的误写/垃圾）→ `curl -X DELETE "http://127.0.0.1:3467/api/entry?id=<uuid>"` —— 该路由在 `web_server.py`，由 nssm 服务 **openmem-web** 提供（`0.0.0.0:3467` 实听），id 不存在返回 `{"ok":false}`，n>0 才真删；**不可逆**。

· 存量重复跑 `mh_dupcheck` 扫（全库两两比，带 tier）。读过原文确认「同题不同事、不许合并」的对记进 PG 表 `dup_exempt`（带理由+判定人），体检自动跳过 —— **判定要有落点**，别反复审同一批老账。

## 会拦你的三道闸门

| 关 | 拦什么 | 怎么过 |
|---|---|---|
| ① 规矩关 | 手上没票（没领过，或那张已用掉） | `mh_skill(agent="<你的名字>")` 领票再写。**票管一条，写成功即作废** |
| ② 查重关 | **你自己** 30min 内没搜过库 | `mh_search(…, requester="<你的名字>")` 后再写 |
| ③ 重复关 | 与库内条目相似度过高（09-25 起分三档；旧值 ≥0.95 全库最高一对才 .9237，那关一直是摆设） | **≥0.88 拒写** → 改 `mh_update` 合并；**≥0.855 拦一次**并把老条目首尾原文贴给你，读完仍判不同 → 原样再交即放行；**≥0.84 放行**但返回体点名要你自判 |

🔴 **分数不配当结论**：0.9889 可以是两个不同服务的台账（同模板不同实体），0.82 可以是同一件事的两份 —— 判同判异只能 `mh_get` 读原文。批量台账族（`source=agent-matrix` 的 services/capabilities）只认「首行同名」，**手写条目不享豁免**。
**三道当场判，无豁免**；③ 另带防死锁阀：同内容被拦过一次、再交即放行（防卡死，不是开口子）。

## 一句话

**规矩是「你自己」的责任，不是集体荣誉** —— 别人领过、别人搜过，不代表你可以跳过。不能只拉屎不擦屁股。
